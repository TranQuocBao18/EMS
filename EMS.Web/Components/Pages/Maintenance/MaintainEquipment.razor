@page "/maintenance/approved/maintain/{ID:int}"

<section class="content-header">
	<div class="container-fluid">
		<div class="row mb-2">
			<div class="col-sm-6">
				<h1>Hoàn tất bảo trì thiết bị</h1>
			</div>
			<div class="col-sm-6">
				<ol class="breadcrumb float-sm-right">
					<li class="breadcrumb-item"><a href="/">Trang chủ</a></li>
					<li class="breadcrumb-item"><a href="/maintenance/approved">Yêu cầu bảo trì thiết bị đã hoàn thành phê duyệt</a></li>
					<li class="breadcrumb-item active">Hoàn tất bảo trì thiết bị</li>
				</ol>
			</div>
		</div>
	</div><!-- /.container-fluid -->
</section>

<div class="card">
	<div class="card-header">
		<h3 class="card-title">Hoàn tất bảo trì thiết bị</h3>
	</div>
	<EditForm OnValidSubmit="Submit" FormName="MaintainEquipment" Model="Model">
		<DataAnnotationsValidator />
		<div class="card-body">
			<div class="row mb-3">
				<div class="col-md-6">
					<label>ID</label>
					<InputNumber class="form-control" @bind-Value="Model.RequestId" disabled="true" />
					<ValidationMessage For="() => Model.RequestId" />
				</div>
			</div>
			<div class="row mb-3">
				<div>
					<label>Ghi chú</label>
					<InputTextArea class="form-control" @bind-Value="Model.Note" />
					<ValidationMessage For="() => Model.Note" />
				</div>
			</div>
			<div class="card-footer">
				<button class="btn btn-primary mr-2" type="submit">Lưu</button>
				<a class="btn btn-secondary" href="/maintenance/approved">Trở về</a>
			</div>
		</div>
	</EditForm>
</div>
